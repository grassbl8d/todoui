// Command todo-ui is a terminal UI for Todoist (Sync API, offline-first).
// The implementation lives in internal/todoui; this is just the entry point.
package main

import "github.com/grassbl8d/todo-ui/internal/todoui"

func main() { todoui.Main() }
